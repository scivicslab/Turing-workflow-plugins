<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.scivicslab.turingworkflow.plugins</groupId>
    <artifactId>turing-workflow-plugins-parent</artifactId>
    <version>4.1.0</version>
    <packaging>pom</packaging>

    <name>Turing Workflow Plugins</name>
    <description>Plugin collection for Turing-workflow engine</description>
    <url>https://github.com/scivicslab/Turing-workflow-plugins</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>scivicslab</id>
            <name>Scivics Lab</name>
            <email>devteam@scivicslab.com</email>
            <organization>Scivics Lab</organization>
            <organizationUrl>https://scivics-lab.com</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/scivicslab/Turing-workflow-plugins.git</connection>
        <developerConnection>scm:git:ssh://github.com:scivicslab/Turing-workflow-plugins.git</developerConnection>
        <url>https://github.com/scivicslab/Turing-workflow-plugins</url>
    </scm>


    <modules>
        <module>plugin-llm</module>
        <module>plugin-prompt-builder</module>
        <module>plugin-secret</module>
        <module>plugin-vault</module>
        <module>plugin-log-db</module>
        <module>plugin-log-output</module>
        <module>plugin-kana-kanji</module>
        <module>plugin-report</module>
        <module>plugin-ssh</module>
        <module>plugin-inventory</module>
        <module>plugin-web</module>
        <module>plugin-chatui3</module>
        <module>plugin-fineweb-search</module>
        <module>plugin-openalex</module>
        <module>plugin-ocr</module>
        <module>plugin-codedoc</module>
    </modules>

    <properties>
        <!-- A module that declares no actions has no turing-workflow dependency, so the
             generator is not on its classpath; such a module sets this to true. -->
        <actionSchemas.skip>false</actionSchemas.skip>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <pojo-actor.version>4.1.0</pojo-actor.version>
        <turing-workflow.version>4.1.0</turing-workflow.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.scivicslab</groupId>
                <artifactId>pojo-actor</artifactId>
                <version>${pojo-actor.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.scivicslab</groupId>
                <artifactId>turing-workflow</artifactId>
                <version>${turing-workflow.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>5.10.2</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>3.25.3</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Build-time only: ActionSchemaGenerator needs it, the artifacts do not. -->
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-generator</artifactId>
            <version>4.38.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <doclint>none</doclint>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>generate-plugin-manifest</id>
                        <goals><goal>javadoc</goal></goals>
                        <phase>prepare-package</phase>
                        <configuration>
                            <!-- Its own output directory. Sharing target/apidocs with
                                 attach-javadocs makes the two executions read each other's
                                 staleness marker: when this one is skipped as up to date it
                                 writes no options file, and attach-javadocs then fails with
                                 "Unable to compute stale date". -->
                            <outputDirectory>${project.build.directory}/plugin-manifest-javadoc</outputDirectory>
                            <reportOutputDirectory>${project.build.directory}/plugin-manifest-javadoc</reportOutputDirectory>
                            <doclet>com.scivicslab.pojoactor.doclet.TuringPluginDoclet</doclet>
                            <docletArtifact>
                                <groupId>com.scivicslab</groupId>
                                <artifactId>turing-workflow</artifactId>
                                <version>${turing-workflow.version}</version>
                            </docletArtifact>
                            <useStandardDocletOptions>false</useStandardDocletOptions>
                            <additionalOptions>
                                <additionalOption>-turingOutputDir</additionalOption>
                                <additionalOption>${project.build.outputDirectory}/META-INF</additionalOption>
                            </additionalOptions>
                            <doclint>none</doclint>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>0.11.0</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                    <autoPublish>true</autoPublish>
                </configuration>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>21</source>
                        <target>21</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>com.scivicslab</groupId>
                                <artifactId>turing-workflow</artifactId>
                                <version>${turing-workflow.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <!-- Generate a JSON Schema for every @Action that declares argsType.
             Runs in a forked JVM with the module's own classpath: exec:java would put the
             generator in the plugin's class loader, which cannot see the module's compiled
             classes' dependencies, and unloadable classes are skipped without a message.
             The profile turns itself on only where there is Java source, so the aggregator
             POM — which compiles nothing — does not run the generator. -->
        <profile>
            <id>generate-action-schemas</id>
            <activation>
                <file><exists>${project.basedir}/src/main/java</exists></file>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>generate-action-schemas</id>
                                <phase>process-classes</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <skip>${actionSchemas.skip}</skip>
                                    <executable>java</executable>
                                    <classpathScope>test</classpathScope>
                                    <arguments>
                                        <argument>-classpath</argument>
                                        <classpath/>
                                        <argument>com.scivicslab.pojoactor.action.schema.ActionSchemaGenerator</argument>
                                        <argument>${project.build.outputDirectory}</argument>
                                        <argument>${project.build.outputDirectory}/action-schemas</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
